const menu=document.querySelector('.menu');const nav=document.querySelector('.site-nav nav');if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.textContent=open?'CLOSE':'MENU';});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.textContent='MENU'}));}const items=document.querySelectorAll('.cards article,.roadmap article,.pipeline div,.coin-panel');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('seen');observer.unobserve(e.target)}}),{threshold:.12});items.forEach((el,i)=>{el.style.transitionDelay=`${Math.min(i*70,420)}ms`;observer.observe(el)});